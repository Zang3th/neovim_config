return {
    "echasnovski/mini.trailspace",
    version = "*",
    config = function()
        local mini_trailspace = require("mini.trailspace")
        mini_trailspace.setup({
            -- default config
        })
    end,
}
